#include <iostream>
using namespace std;
int main()
{ // 1 rercentage
    // float x1 = 99, x2 = 98, x3 = 97, x4 = 96, x5 = 95;
    // float percent = (x1 + x2 + x3 + x4 + x5) / 5;
    // cout << percent;
    // 2.area of circle
    // float r = 8, pi = 3.1415;
    // float area = r * r * pi;
    // cout << area;
    // 3.simple interest
    // float P = 100, R = 3.5, T = 10;
    //  float si= (P * R * T) / 100;
    // cout << si;
    // return 0;
    
}
